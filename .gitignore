# Batch API outputs (large, regenerable)
openAI_batch_output/
openAI_batch_input/
claude_batch_output/
batch_input.jsonl

# Paper collection metadata
paper_collection/

# Secrets / credentials
.env
extraction/gdrive_credentials.json
extraction/gdrive_token.json

# System files
.DS_Store
.mplconfig/

# Python artifacts
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv*/
.python-version

# Extraction pipeline outputs (large, regenerable)
extraction/output_csv/
extraction/output_xlsx/
extraction/outputs/
extraction/inputs/
extraction/plots/
extraction/gdrive_token_rw.json

# PGG papers (downloaded from Google Drive, not source-controlled)
PGG_papers/papers/
PGG_papers/7_papers/

# Large result files (regenerable by running prediction pipeline)
results/extraction_model_comparison/.embedding_cache.pkl

# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz

# Evaluator data dirs (populated locally, not committed)
evaluator/inputs/human/
evaluator/inputs/llm/
evaluator/outputs/
evaluator/data/
evaluator/utils/embedding_cache.json
evaluator/notebooks/

# Extraction app data (annotations + papers + guide → delivered via Drive)
extraction_app/extractions.json
extraction_app/pdfs/
extraction_app/Human_Extraction_Guide.docx

# Test-set evaluation outputs (raw model responses → delivered via Drive)
benchmarks/test_set_eval/raw_claude_responses.json
benchmarks/test_set_eval/results/

# Local Deliverables staging area (uploaded to Drive, not committed)
Deliverables/
scripts/drive_manifest.yaml

# Literature pipeline outputs (paper analysis reports → delivered via Drive)
literature/output/

# Notebooks figure outputs (regenerable; large image directories)
notebooks/crosswave/
notebooks/granular/
notebooks/learning/
notebooks/paper/
notebooks/positive_cases/
notebooks/validation/

# Experiment figures data layer (csv/figure outputs)
experiment_figures/data/
experiment_figures/.ipynb_checkpoints/

# Empirical PGG data files
science_data/data/

# Prediction pipeline cached pickles / batch outputs
prediction/**/__pycache__/
prediction/**/*.pkl

# CoDa benchmark data (raw dataset, not redistributable)
benchmarks/coda/data/coda_raw.csv
benchmarks/coda/batch_outputs/
